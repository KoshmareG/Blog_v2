<div class="container">
    <div class="row justify-content-md-center">
        <div class="col col-xs-4 col-lg-8">
            <div class="card mb-2 shadow">
                <div class="card-body">
                    <%= form_for @article do |f| %>
                    <h4 class="card-title">Редактирование статьи</h5>
                    <h5 class="form-label">Заголовок</h6>
                    <%= f.text_field :title, class: "form-control mb-1" %>
                    <h5 class="form-label">Текст статьи</h6>
                    <%= f.text_area :text, class: "form-control mb-3", rows: "16", id: "scrollbar" %>
                    <%= f.button "Сохранить", type: "submit", class: "btn btn-outline-success btn-sm" %>
                    </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
